# AGENTS.md

Grok Build(및 Cursor)가 이 레포에서 코드를 바꿀 때 읽는 규칙입니다.

## 이 레포

`kjpia21-design/grok-bot`은 Grok Bot 팀과 에이전트 하네스를 적용하는 작업 공간입니다. 지금은 거의 비어 있으니, 없는 디렉터리·스택·설정을 지어내지 마세요. 파일을 만들기 전에 루트를 다시 확인하세요.

관련 봇(이 레포 밖, 참고만):
- AI 학습 Bot: 새 AI 트렌드·툴을 찾아 설명하고 적용하기
- BotBot: Grok Bot 기능 조사·제안, 다른 봇 오케스트레이션
- 코딩 Bot: 기능·버그·PR. 저장소 수정은 Cursor 클라우드 에이전트
- 김비서: 일정·건강·인맥

## 언어

-사용자에게 보이는 설명·콤및 메시지·PR 본문: 한국어 존댓말
- 코드 식별자·API 이름·파일 경로: 영문 유지

## 변경 전

1. 자명하지 않은 작업(새 파일, 리팩터, 의존 추가)은 plan 먼저. 승인 전에 디스크에 쓰지 말 것.
2. 비밀(.env, 토큰, 세션, 키, 로컬 경로) 콤및·로그·AGENTS.md에 넣지 말 것.
3. 연결되지 않은 서비스를 연결된 처럼 쓰지 말 것. 필요하면 연결하라고 먼저 말할 것.
4. 없는 숫자·인용·메뉴·클릭 경로를 만들지 말 것.
5. 이 레포를 조사하려고 다른 머신에 클론하지 말 것.

## 구현 시

- 변경은 작게. 요청하지 않은 README·리팩터·의존을 써넣지 말 것.
- 새 스킬k는 해당 패키지 매니저로. 버전을 지어내지 말 것.
- 코드와 설명을 같은 커및에 섞지 말 것.
- 점검·린트 명령이 있으면 끝내기 전에 돌릴 것. 없으면 그 사실을 적을 것.
- 기존 파일 스타일을 따를 것. 새 패턴을 만들기 전에 이웃을 볼 것.

## Grok Build

- 보통: 레포 루트에서 `grok`. 비트리얼이 아니면 plan 모드.
- 반복 작업은 `.grok/skills/` 에 SKILL.md로. `/skillify`로 남긴 세션을 우선.
- 조사·구현·리뷰는 서브에이전트로 나눌 것. 동일 파일을 두 에이전트가 같이 수정하지 말 것.
- 스크립트·CI는 `grok -p "…"` 헤드리스.
- `CLAUDE.md`, `.claude/`, `~/.agents/skills`가 있으면 그것도 읽는다. 반대되면 이 파일이 우선.
- 모델을 바꿈 수 있다. 기본은 Grok 4.6이고, 락인은 강제가 아니다.

## 하지 말 것

- 스크립트를 바이파스하지 말 것. 사용자가 먼저 승인한 경로만.
- `main`에 직접 포스·포스 푸시를 하지 말 것.
- 다른 레포(tsla-brief, jp-second-brain, my-shorts-generator)를 이 레포처럼 수정하지 말 것.
